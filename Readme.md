this repository is outdated, as it is not even possible to install according to the instructions of the authors.  


```sh
~/bevformerproj/mmdetection3d$ FORCE_CUDA=1 python3 setup.py install
```  


```sh
raise ValueError(f"Unknown CUDA arch ({arch}) or GPU not supported")
ValueError: Unknown CUDA arch (9.0) or GPU not supported
```

DEPENDENCY HELL REPO  

